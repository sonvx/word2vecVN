These special character is deleted from "out.json" file:
- For English data:

\
"
'

(For example, delete all character "\": replace all "\" by "")

- For Japanese data:
"　\"                (convert to "")
""（"                (convert to "")